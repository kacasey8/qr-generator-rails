<%= form_with(model: qr_link) do |form| %>
  <% if qr_link.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(qr_link.errors.count, "error") %> prohibited this qr_link from being saved:</h2>

      <ul>
        <% qr_link.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :destination_url, style: "display: block" %>
    <%= form.text_field :destination_url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
